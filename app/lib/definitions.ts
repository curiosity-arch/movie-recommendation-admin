export type User = {
    id: string;
    username: string;
    password: string;
    tahun_lahir: number;
};

export type Admin = {
    id: string;
    username_admin: string;
    password_admin: string;
};

export type UserId = {
    id: string;
    username: string;
    password: string;
    tahun_lahir: number;
}

export type AdminId = {
    id: string;
    username_admin: string;
    password_admin: string;
}